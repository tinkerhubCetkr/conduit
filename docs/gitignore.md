# .gitignore & .gitkeep

A *.gitignore* file tells Git which files or folders should NOT be tracked. Unnecessary files will not get committed.

Git does not track empty folders.To keep an empty folder in the repository and get that file tracked, place an empty file named: *.gitkeep*
