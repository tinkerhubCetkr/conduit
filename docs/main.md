# Main

This is the main file that is used by the user, and holds the start of the program chain.

**WorkFlow**
-   Program initially checks its a non dependent program then moves on to running the script

**Responsibilities**
-   Gather Raw user input and then calls the validator function (first_phase_checker).

